# Optional build-time configuration (do not commit real secrets)
#
# 推荐生产环境不设置 VITE_QB_URL，默认走同源（静态资源与 qB WebUI 同域时最安全）。
# 如确需跨域（不推荐），需要同时设置 VITE_ALLOW_CROSS_ORIGIN=true。
#
# VITE_QB_URL=http://localhost:8080
# VITE_ALLOW_CROSS_ORIGIN=false

# Transmission RPC 地址
# 推荐生产环境不设置 VITE_TR_URL，默认走同源（静态资源与 Transmission 同域时最安全）。
# 注意：如果你的 WebUI 静态站点不是由同一个域名反代到 Transmission，则需要显式设置 VITE_TR_URL（或配置反向代理将 /transmission/rpc 转发到 Transmission）。
# VITE_TR_URL=http://localhost:9091/transmission/rpc

# 强制指定后端类型（跳过自动检测）: qbit | trans | auto
# VITE_BACKEND_TYPE=auto
